(eyebrowse-mode t)
;; use desktop-mode for restoring session; one desktop is enough
(desktop-save-mode 1)
(setq desktop-path '("~/.emacs.d/"))
(setq desktop-dirname "~/.emacs.d/")
(setq desktop-base-file-name "emacs-desktop")
(provide 'init-eyebrowse)
